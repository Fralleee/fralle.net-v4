# Drives the TopNav status row + the HeaderRail availability badge.
# Set to "false" when not currently job-hunting; any other value (or
# unset) keeps the "Open to new roles" copy + pulse dot. Toggle on
# Vercel via Settings -> Environment Variables and trigger a redeploy.
PUBLIC_AVAILABLE_FOR_HIRE=true
