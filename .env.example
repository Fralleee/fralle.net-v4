# Drives the TopNav status row, HeaderRail availability badge, and the
# About section's job-seeking copy. Only the literal value "true" turns
# them on; any other value (or unset) renders the not-currently-looking
# variant. Toggle on Vercel via Settings -> Environment Variables and
# trigger a redeploy.
PUBLIC_AVAILABLE_FOR_HIRE=false

# PostHog access for the build-time sparkline data refresh
# (scripts/generate-sparklines.ts). Generate a personal API key at
# https://eu.posthog.com/settings/user-api-keys with the `insight:read`
# scope. When any of these are unset, the script logs a warning and
# falls back to the last-committed JSON in src/data/sparklines/.
POSTHOG_HOST=https://eu.posthog.com
POSTHOG_PROJECT_API_KEY=phx_...
POSTHOG_PROJECT_ID=...
